"""
==============================================
  Stage 5 — 関数パイプライン（激ムズ）
==============================================
  暗号方式: 関数定義 + map + ソート + 逆順
  目標: 関数を定義し、複数ステップを組み合わせて解読する
==============================================
"""
from verify import check

# 暗号データ（0始まりのアルファベット番号、ランダム順）
data = [14, 12, 4, 6, 0]


def to_letter(n):
    """
    数値をアルファベットに変換する関数

    【やること】
    0 → 'A', 1 → 'B', ... 25 → 'Z' に変換する

    【ヒント】
    - chr(65) は 'A'
    - chr(65 + 0) → 'A', chr(65 + 1) → 'B', ...
    """
    # ===== ここを埋めてください =====
    return ""  # ← chr() と n を使って文字を返す
    # ================================


def decode(data):
    """
    多段パイプラインでパスフレーズを復号する関数

    【やること】
    1. data をソートする（昇順）
    2. 各数値を to_letter() で文字に変換する（map を使う）
    3. 結合して文字列にする
    4. 文字列を逆順にする

    【ヒント】
    - sorted() でソート
    - map(関数, リスト) で各要素に関数を適用
    - "".join() で結合
    - 文字列[::-1] で逆順
    """

    # ===== ここを埋めてください =====
    # Step 1: ソート
    step1 = []  # ← sorted() を使う

    # Step 2: 各数値を文字に変換（map + to_letter）
    step2 = []  # ← map() を使う

    # Step 3: 結合
    step3 = ""  # ← join() を使う

    # Step 4: 逆順
    step4 = ""  # ← [::-1] を使う
    # ================================

    return step4


# 実行
if __name__ == "__main__":
    answer = decode(data)
    print(f"暗号データ: {data}")
    print(f"復号結果: {answer}")
    print()

    if check(5, answer):
        print("✓ 正解！ この復号結果をブラウザの「パスフレーズ入力」に入力してください！")
    else:
        print("✗ 不正解… decode関数を見直してみましょう！")
