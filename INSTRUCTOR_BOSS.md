# ボス戦・モブバトル — 講師用ガイド

EXTRA ステージで出現する **ボス戦** と、フィールド徘徊敵との **モブバトル（Python クイズ）** をまとめたものです。

---

## ボス戦 — PHOENIX GUARDIAN

EXTRA ステージ（floor 6+）クリア時に出現するラスボス。
近づくとモーダルが開き、3 問のコーディング問題を順に解く戦闘システム。

### 戦闘ルール

| 項目 | 値 |
|---|---|
| 出現条件 | Stage 6 EXTRA でステージクリア後 |
| ボス HP | **3**（正解 1 回 = 1 ダメージ） |
| 制限時間 | **15 分** (900 秒)。タイムアウトで再挑戦に戻る |
| ボスの攻撃 | 距離 3〜25 で 2〜3.5 秒ごとに火球発射、被弾で 40〜64 ダメージ |
| 撃破報酬 | 脱出ポータル出現 → ゲームクリアへ |

> **判定方法**: コード実行結果（`print` 出力）が期待値と**完全一致**すれば 1 ダメージ。
> 3 問全部正解で撃破。

---

### 第 1 問 — 成績処理関数

**問題**
```
引数: 点数のリスト scores
処理:
  1. 50点未満の不合格者を除外
  2. 合格者の点数を昇順ソート
  3. ソート済みリストを返す
```

**テンプレート（1 箇所埋める）**
```python
def process(scores):
    passed = [s for s in scores if _____]
    passed.sort()
    return passed

print(process([30, 85, 42, 90, 55, 10, 78, 63]))
```

| | |
|---|---|
| 空欄の答え | `s >= 50` |
| 期待出力 | `[55, 63, 78, 85, 90]` |

**解説**: リスト内包表記のフィルタ条件に `s >= 50`。30/42/10 を除外し、残り [85, 90, 55, 78, 63] を昇順ソート。

---

### 第 2 問 — シーザー暗号

**問題**
```
引数: 文字列 text, シフト数 n
処理:
  1. 各文字のASCIIコードに n を加算
  2. 変換後の文字を結合して返す
  3. アルファベット以外はそのまま
```

**テンプレート（1 箇所埋める）**
```python
def encode(text, n):
    result = ""
    for ch in text:
        if ch >= "A" and ch <= "Z":
            code = ord(ch) - ord("A")
            new_code = _____ % 26
            result += chr(new_code + ord("A"))
        else:
            result += ch
    return result

print(encode("HELLO WORLD", 7))
```

| | |
|---|---|
| 空欄の答え | `(code + n)` |
| 期待出力 | `OLSSV DVYSK` |

**解説**:
- `code` は 0〜25（A=0, B=1, ..., Z=25）に正規化
- `(code + n) % 26` で Z を超えたら A に巡回
- 例: `W(22)+7=29%26=3→D`、`H(7)+7=14→O`
- スペースは `if` 条件外なので `else` でそのまま追加

---

### 第 3 問 — データ集計

**問題**
```
引数: 名前と点数のペアのリスト
処理:
  1. 各名前の合計点を計算
  2. 合計点が最も高い人の名前を返す
```

**テンプレート（1 箇所埋める）**
```python
names = [["Alice",80],["Bob",90],["Alice",70],["Bob",60],["Alice",50]]
totals = {}
for pair in names:
    name = pair[0]
    score = pair[1]
    if name in totals:
        totals[name] += score
    else:
        totals[name] = score
best = ""
best_score = 0
for name in totals:
    if totals[name] > _____:
        best = name
        best_score = totals[name]
print(best)
```

| | |
|---|---|
| 空欄の答え | `best_score` |
| 期待出力 | `Alice` |

**解説**:
- 集計: `Alice = 80+70+50 = 200`、`Bob = 90+60 = 150`
- ループで「現在のベストスコア (`best_score`)」と比較、上回れば更新
- Alice (200) > 0 → 採用、Bob (150) > 200 は false → 不採用
- 結果: `Alice`

---

### ボス戦 早見表

| 問 | 空欄の答え | 出力 |
|---|---|---|
| 第1問 成績処理 | `s >= 50` | `[55, 63, 78, 85, 90]` |
| 第2問 シーザー暗号 | `(code + n)` | `OLSSV DVYSK` |
| 第3問 データ集計 | `best_score` | `Alice` |

> **戦闘ヒント**: ボスから 2.5 マス以内に近づくと自動的にバトルモーダルが開きます。火球は壁で消えるので柱・壁を盾にしながら近づくと安全。

---

## モブバトル — Python クイズ（90 問プール）

通路を徘徊する敵（goblin/orc/demon など）に接触すると Python クイズが開始されます。

### バトルルール

| 項目 | 内容 |
|---|---|
| 出題形式 | 4 択 (`choice`) または穴埋め (`fill`) |
| 1 戦闘の問題数 | 敵の HP に応じて 1〜3 問 |
| 正解時 | 敵にダメージ |
| 不正解・時間切れ | プレイヤーがダメージ |
| 撃破報酬 | XP + HP 自動回復 |
| 逃走 | 各階層 2 回まで（HP -25% + XP 減） |
| 難易度フィルタ | プレイヤーの階層 / 敵タイプで `easy` / `normal` / `hard` を抽選 |

### 問題プール構成（[js/questions.js](js/questions.js) 全 90 問）

| 難易度 | 問題数 | 主な範囲 |
|---|---|---|
| easy | 〜45 | リスト基本／文字列／演算／比較／bool／型変換／ループ／代入 |
| normal | 〜25 | 内包表記／辞書／lambda／enumerate/zip/map／三項演算子／集合 |
| hard | 〜20 | デコレータ／ジェネレータ／クロージャ／継承／ビット演算／walrus／Counter |

### 出題サンプル

**easy 例**
- `print(2 ** 3)` の出力は？ → **8**
- `print("Ha" * 3)` の出力は？ → **HaHaHa**
- 文字列の長さは？ `len(s)`

**normal 例**
- `[x**2 for x in range(5) if x % 2 == 0]` → **[0, 4, 16]**
- `sum(d.values())` where `d={'a':1,'b':2,'c':3}` → **6**

**hard 例**
- デコレータの実行順序問題
- フィボナッチジェネレータの最初の 5 要素
- walrus 演算子 `(n := len(data))` の挙動

### 講師の役割

- 学生が同じ問題で何度も詰まる場合、`questions.js` 内の `expl` フィールドに簡潔な説明があるので、それを基に補足する
- 難易度感を変えたい場合は `js/questions.js` の `diff` フィールドを編集
- 全 90 問の詳細は [js/questions.js](js/questions.js) に直接記載されているので、必要時に grep / 閲覧してください
