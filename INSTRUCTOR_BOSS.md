# ボス戦・モブバトル — 講師用ガイド

EXTRA ステージ（Stage 6 / Floor 6）のラスボス **ROBOT ARENA — フェニックスガーディアン** と、フィールド徘徊敵との **モブバトル（Python クイズ）** をまとめたものです。

---

## ボス戦 — ROBOT ARENA（フェニックスガーディアン）

EXTRA ステージの暗号 (`PHOENIX` / `ALPHA-7`) をクリア後にポータルへ近づくと突入。
**ターン制の AI 対戦** で、学生は自前で書いた Python の `robot_action(state)` 関数でロボットの行動を制御してボスを倒します。

### 戦闘の全体像（フル画面モーダル）

```
┌────────────────────────────────────────────────────┐
│ TURN N                                             │
│ 🤖 ロボット HP/エネルギー   |   👹 ボス HP        │
│ 攻撃力 / 防御力 / 速さ / 回避率 / クリティカル率 / 軽減 / 必殺CD │
├──────────────────┬─────────────────────────────────┤
│ 行動リファレンス   │ 🐍 Python エディタ              │
│  attack          │   def robot_action(state):     │
│  ranged          │       boss = state["boss_action"]│
│  defend          │       ...                       │
│  dodge           │                                 │
│  special         │   ▶ 実行（3 ターン進む）        │
│  charge          │                                 │
│  ─ ボス行動 ─    │ ── 戦闘ログ ──                  │
│  attack/heavy/   │   Turn 1 / Turn 2 / Turn 3 ...  │
│  charge/heal/    │                                 │
│  shield          │                                 │
└──────────────────┴─────────────────────────────────┘
```

### ルールサマリ

| 項目 | 内容 |
|---|---|
| 出現条件 | Stage 6 EXTRA クリア → ポータル接触 |
| 入力 | Python の `robot_action(state)` を書く（ターン中も書き換え可） |
| 1 押し | `▶ 実行` で **3 ターンまとめて進行** |
| 予告 | バッチ **1 ターン目だけ** 画面上部に「次のボス行動」が出る。2-3 ターン目はランダム |
| 中央バナー | 各ターン開始時に **「Nターン目」+ ロボット行動 + ボス行動** を中央にドンと表示 |
| ボス HP | **560**（装備ありで攻撃を回避＋削るのを 10〜20 ターンくらい繰り返す想定） |
| ボス ATK | 34 / DEF 12 |
| 物理攻撃（火球・雷撃） | **ボスには効かない** ／ 接触時「💢 ボスに物理攻撃は効かないようだ…」表示 |
| 撃破時 | 🏆 ボス撃破! バナー → TRUE ESCAPE 画面 |
| 敗北時 | 「もう一度」/「離れる」の選択肢（装備は保持して再戦可） |

### 装備（アイテム）と特殊効果

ロボットは Floor 1〜5 の **暗号ターミナル Phase 2 クリア後** に落ちるアイテムを各フロア 1 つずつ拾って組み立てます。全部集めると Stage 5 の暗号クリア時に **組み立て演出**（中央暗転 + メッセージ）が走り、右下の HUD が 5 スロットから **🤖 1 アイコン** に変わります。

各装備には **★ マークの特殊効果**（戦闘中ずっと適用される）が付いています:

| フロア | カテゴリ | 例 | 特殊効果（抜粋） |
|---|---|---|---|
| 1 | ボディ | 軽量 / 重装 / 標準 | 回避特化 / 被ダメージ -6 / クリティカル |
| 2 | 武器 | ブレード / ライフル / ハンマー | 通常攻撃 1.8× / ranged コスト減 / シールド貫通 |
| 3 | サブ武器 | ミサイル / ビーム / 火炎 | 必殺火力上乗せ / 必殺コスト -8 / 燃焼継続 |
| 4 | 移動 | ジェット / ホイール / ホバー | 回避強化 / charge 回復 +12 / 防御中 HP 回復 |
| 5 | コア | 核融合炉 / バッテリー / 量子 | 自動 EN +5/turn / 自動 HP / 全方位ブースト |

### ロボットの行動（`return {"action": ...}`）

| 行動 | エネルギー | 効果 |
|---|---|---|
| `"attack"` | 0 | 通常攻撃（コストなし、中ダメージ） |
| `"ranged"` | **10** | 遠距離射撃（攻撃の +50% 程度、ライフル装備で -6） |
| `"defend"` | 0 | 防御態勢（被ダメージを大きく減らす） |
| `"dodge"` | 0 | 回避（速さ依存で成功） |
| `"special"` | **20** | 必殺技（**3 ターンのクールタイム**、サブ武器で効果変化） |
| `"charge"` | — | エネルギー回復（9 + 速さ/3、ホイール +12） |

> 各ターンの **自動回復**: 通常 +1 / 核融合炉あり +5

### ボスの行動（`state["boss_action"]` でこのターンの予告を読める）

| boss_action | 挙動 |
|---|---|
| `"attack"` | 通常攻撃 |
| `"heavy"` | 大攻撃（ダメージ大） |
| `"charge"` | 力を溜める（次に heavy が来やすい） |
| `"heal"` | 自己回復 32〜48 |
| `"shield"` | 防御（こちらの攻撃半減） |

> **適応 AI**: special を 2 回連続で撃つとボスが shield / charge を高確率で出してくる。heavy 直後は固まりやすいので dodge / defend が安全。

### ロボットの戦略テンプレ（学生に見せて OK）

```python
def robot_action(state):
    boss = state["boss_action"]
    hp_ratio = state["my_hp"] / state["my_max_hp"]

    # ボスが大攻撃の予告を出したら回避
    if boss == "heavy":
        return {"action": "dodge"}

    # HP が削れたら防御
    if hp_ratio < 0.3:
        return {"action": "defend"}

    # 必殺技が撃てるなら撃つ
    if state["special_cd"] == 0 and state["my_energy"] >= 20:
        return {"action": "special"}

    # 普段は遠距離射撃
    return {"action": "ranged"}
```

### よくある詰まり

| 現象 | 助言 |
|---|---|
| `boss == "heavy"` なのに dodge しない | バッチ 2-3 ターン目は予告がランダムなので、コードでは検知可能だが UI には出ない。1 ターン目のみ画面上部に予告が出る |
| `if hp_ratio < 0.3 and boss = "attack"` でエラー | `=` が代入。比較は `==` 2 つ |
| 火球・雷撃でボスを削ろうとする | 「ボスに物理攻撃は効かないようだ…」と出る。コードで倒すしかない |
| エネルギーが尽きて special が撃てない | `"charge"` を 1 ターン挟む。`核融合炉` 装備で自動回復 +5/turn |
| ボスが回復連発 | HP 半分以下で heal/shield を多用する適応 AI。先に削るリズムを変える |

### バランス調整の勘所

- ボス HP/ATK は `BOSS_BASE` ([js/robotBattle.js:23](js/robotBattle.js#L23)) で定義
- エネルギーコスト / クールタイムは同ファイルの `resolveOneTurn` 内
- 装備の特殊効果は [js/items.js](js/items.js) の `ITEM_CATEGORIES`

---

## モブバトル — Python クイズ（90 問プール）

通路を徘徊する敵（goblin/orc/demon など）に接触すると Python クイズが開始されます。

### バトルルール

| 項目 | 内容 |
|---|---|
| 出題形式 | 4 択 (`choice`) または穴埋め (`fill`) |
| 1 戦闘の問題数 | 敵の HP に応じて 1〜3 問 |
| 正解時 | 敵にダメージ |
| 不正解・時間切れ | プレイヤーがダメージ |
| 撃破報酬 | XP + HP 自動回復 |
| 逃走 | 各階層 2 回まで（HP -25% + XP 減） |
| 難易度フィルタ | プレイヤーの階層 / 敵タイプで `easy` / `normal` / `hard` を抽選 |

### モブ密度モード（タイトル画面で選択）

| モード | スポーン量 | 想定 |
|---|---|---|
| 🟢 EASY | 少なめ | Python 初学者・短時間セッション |
| 🟡 NORMAL | 標準 | デフォルト |
| 🔴 HARD | 多め | 上級者・経験のあるチーム |

> スコアではモブ撃破ポイントが **上限なし**（`kills × 8`）。HARD でモブを倒しまくっても理不尽にならないように調整済み。

### 問題プール構成（[js/questions.js](js/questions.js) 全 90 問）

| 難易度 | 問題数 | 主な範囲 |
|---|---|---|
| easy | 〜45 | リスト基本／文字列／演算／比較／bool／型変換／ループ／代入 |
| normal | 〜25 | 内包表記／辞書／lambda／enumerate/zip/map／三項演算子／集合 |
| hard | 〜20 | デコレータ／ジェネレータ／クロージャ／継承／ビット演算／walrus／Counter |

### 出題サンプル

**easy 例**
- `print(2 ** 3)` の出力は？ → **8**
- `print("Ha" * 3)` の出力は？ → **HaHaHa**
- 文字列の長さは？ → `len(s)`

**normal 例**
- `[x**2 for x in range(5) if x % 2 == 0]` → **[0, 4, 16]**
- `sum(d.values())` where `d={'a':1,'b':2,'c':3}` → **6**

**hard 例**
- デコレータの実行順序問題
- フィボナッチジェネレータの最初の 5 要素
- walrus 演算子 `(n := len(data))` の挙動

### 講師の役割

- 学生が同じ問題で何度も詰まる場合、`questions.js` 内の `expl` フィールドに簡潔な説明があるので、それを基に補足する
- 難易度感を変えたい場合は `js/questions.js` の `diff` フィールドを編集
- 全 90 問の詳細は [js/questions.js](js/questions.js) に直接記載されているので、必要時に grep / 閲覧してください
