# 講師実行手順書（Runbook）

セッション当日に**この通り順番にやるだけ**でゲームを運営できるようにまとめた手順書。

---

## 📋 全体の流れ

```
[事前] 環境チェック
   ↓
[1] 講師PCでサーバー起動 ────────────┐
   ↓                                │ ターミナル①（開きっぱなし）
[2] cloudflared でトンネル開設 ─────┐│
   ↓                              │└────────────────────────
[3] ダッシュボードを開く          │ ターミナル②（開きっぱなし）
   ↓                              └──────────────────────────
[4] 学生に URL を共有
   ↓
[5] 学生が「📡 進捗共有を設定」
   ↓
[6] セッション中の監視 + 30 分ごとバックアップ
   ↓
[7] 緊急時の対応
   ↓
[8] セッション終了処理
```

---

## 🔧 事前準備（前日〜30 分前）

### A. 必要なツールがあるか確認

ターミナルで以下を打ち、両方とも応答があれば OK:

```bash
node --version          # → v18.x 以上推奨
which cloudflared       # → /opt/homebrew/bin/cloudflared 等が出ればOK
```

cloudflared が無ければ:
```bash
brew install cloudflared
```

### B. プロジェクトディレクトリを最新化

```bash
cd /Users/seomasato/escape_game
git pull
```

### C. 動作確認（任意・推奨）

```bash
node tracker/server.js
```
別ターミナルで `curl -I http://localhost:9876/export-all` を打ち、`HTTP/1.1 200 OK` が返れば全機能正常。確認できたら `Ctrl+C` で止めて本番に備える。

---

## 🚀 セッション開始時の手順

### ステップ 1: サーバー起動（ターミナル①）

新しいターミナルを開いて以下を実行:

```bash
cd /Users/seomasato/escape_game
node tracker/server.js
```

成功すると下のような出力が出る:

```
進捗トラッカー受信サーバー起動
  PORT: 9876

  講師ダッシュボード（このPC上で開く）:
    http://localhost:9876/dashboard
...
```

**このターミナルはセッション中ずっと開きっぱなしにする**（閉じるとサーバーが止まる）。

### ステップ 2: cloudflared でトンネル開設（ターミナル②）

**別の新しいターミナル**を開いて以下を実行:

```bash
cloudflared tunnel --url http://localhost:9876
```

数秒〜10 秒後、以下のような出力が現れる:

```
+--------------------------------------------------------------------------------------------+
|  Your quick Tunnel has been created! Visit it at:
|  https://abcd-efgh-ijkl-mnop.trycloudflare.com
+--------------------------------------------------------------------------------------------+
```

この **`https://*.trycloudflare.com`** の URL をメモしておく（以下「**公開URL**」と呼ぶ）。

**このターミナルもセッション中ずっと開きっぱなしにする**。

> ⚠️ quick tunnel は再起動するたびに URL が変わります。途中で URL が変わると localStorage が別オリジン扱いになるので、可能なら**セッション中は絶対に止めない**こと。

### ステップ 3: ダッシュボードを開く

ブラウザの新しいタブで以下にアクセス:

```
http://localhost:9876/dashboard
```

または公開 URL 経由でも OK:

```
https://abcd-efgh-ijkl-mnop.trycloudflare.com/dashboard
```

下のような画面が出れば成功:

```
📊 研修進捗ダッシュボード
2秒ごとに自動更新 / 0 チーム接続中 / 最終取得: 10:30:15

[🔄 今すぐ更新] [📥 全チーム最新状態をダウンロード] [🗑️ 全データクリア]
💡 緊急時用: 各行の「📋」でそのチームの復元コマンドをコピーできます

まだ接続しているチームがありません。
ゲームの設定でチーム名とアップロード URL を設定してもらってください。
```

**この画面もセッション中ずっと開きっぱなしにする**。

### ステップ 4: 学生に共有

下記を Slack / ホワイトボード / メール等で共有:

```
ゲーム URL: https://abcd-efgh-ijkl-mnop.trycloudflare.com/dragon.html

設定:
1. タイトル画面の「📡 進捗共有を設定」ボタンを押す
2. チーム名を入力（例: チームA）
3. アップロード URL は自動入力されています、そのままOKを押してください
```

### ステップ 5: 学生接続を待つ

学生が設定を終えると、ダッシュボードの **接続中チーム数** が増えていく:

```
📊 研修進捗ダッシュボード
2秒ごとに自動更新 / 3 チーム接続中 / 最終取得: 10:42:15
```

全チーム揃ったら **セッション開始**。

---

## 🎮 セッション中の運用

### 監視ポイント

ダッシュボードは 2 秒ごとに自動更新される。以下を見ながら適宜介入:

| 確認項目 | 異常サイン |
|---|---|
| **接続中チーム数** | 突然減ったらネットワーク問題か誰かのブラウザが落ちた |
| **更新時刻** | 1 分以上更新がないチームは薄色表示になる |
| **Stage 列** | 他のチームと大きく遅れているチームがいないか |
| **修理列** | 修理が極端に進まないチームは詰まっている |
| **状況列** | 「☠ 強制休憩」が続いていないか |

### 30 分ごとのバックアップ（**必須**）

ダッシュボード上部の **📥 全チーム最新状態をダウンロード** ボタンを押す。

ダウンロードフォルダに以下のファイルが保存される:
```
cipher-dungeon-all-teams-2026-05-12T10-30-00.json
```

これがあれば**どんな事故が起きても全チーム復元可能**。30 分ごとに必ず押す。

### 介入が必要なケース別の対処

#### ケース A: あるチームだけ遅れている

そのチームの PC で DevTools を開いてもらう（`F12` → Console）:

```js
admin.setStage(3); admin.setFloor(3); admin.setLevel(5); admin.unlockAll(2); admin.heal();
```

これで他チームと同じ進度に追いつく。

#### ケース B: 修理ターミナルで詰まっている

```js
admin.unlock("attack", 3)         // 特定の機能だけ解放
// または
admin.unlockAll(3)                // 全機能を最強で解放
```

#### ケース C: HP 0 で動けない

```js
admin.heal()                      // HP/MP 全回復
// または
admin.god()                       // 無敵モード
```

#### ケース D: 暗号が解けず次に進めない

```js
admin.solveCipher()               // 現ステージをクリア扱い + 脱出口出現
// または
admin.skipFloor()                 // 強制的に次階層へ
```

#### ケース E: ブラウザがクラッシュ／フリーズ

1. **ダッシュボードでそのチームの行の 📋 ボタンをクリック**
2. 「✅ 復元コマンドをクリップボードにコピーしました」と表示される
3. 学生に DevTools コンソール (F12) で **Cmd+V → Enter** してもらう
4. ページがリロードされて完全復活

---

## 🚨 緊急事態の対応

### 状況 1: ダッシュボードが急に空になった

サーバーが何らかの理由で再起動された可能性。

1. ターミナル①を確認 → サーバーが止まっていたら再起動:
   ```bash
   node tracker/server.js
   ```
2. ターミナル②の cloudflared が**まだ生きていれば** URL は変わらない
3. 30 秒以内に各チームが再アップロードしてダッシュボードに復帰

### 状況 2: cloudflared が切れて URL が変わった

これは最も厄介なパターン。

1. ターミナル②で再起動:
   ```bash
   cloudflared tunnel --url http://localhost:9876
   ```
2. **新 URL** をメモ
3. **新 URL を学生に再共有**（Slack 等）
4. 学生は新 URL でゲームを開く → タイトル画面で「進捗共有を設定」を**再入力**
5. 各チームの状態を **直近の `📥` JSON** または **ダッシュボードの 📋** から復元

### 状況 3: 完全に環境が死んだ

直近の **📥** でダウンロードした `cipher-dungeon-all-teams-*.json` があれば全て復旧可能:

1. JSON をテキストエディタで開く
2. `teams` 配列の中から復元したいチームの `snapshot` を見つける
3. その PC のコンソールで:
   ```js
   admin.importSave('{"save":"<save の値>","features":"<features の値>"}');
   location.reload();
   ```

---

## 🏁 セッション終了

### 終了処理

1. ダッシュボードで **📥 全チーム最新状態をダウンロード** を**最後にもう一度**押す（最終結果の保存）
2. ターミナル②で `Ctrl+C`（cloudflared 停止）
3. ターミナル①で `Ctrl+C`（サーバー停止）

### データの後処理

ダウンロードした最終 JSON を:
- 振り返り資料として保管
- 各チームの最終スコア／レベル／撃破数を集計

---

## ✅ 開始前チェックリスト

印刷して机に置く用:

- [ ] `node --version` で Node.js 確認
- [ ] `which cloudflared` で cloudflared 確認
- [ ] `cd /Users/seomasato/escape_game && git pull` で最新化
- [ ] **ターミナル①**: `node tracker/server.js` 起動
- [ ] **ターミナル②**: `cloudflared tunnel --url http://localhost:9876` 起動
- [ ] 公開 URL をメモ
- [ ] ダッシュボードを `http://localhost:9876/dashboard` で開く
- [ ] 学生用 URL `https://xxx.trycloudflare.com/dragon.html` を Slack 等で共有
- [ ] 30 分タイマーをセット（バックアップ用）

---

## 📌 重要 URL まとめ

| 用途 | URL |
|---|---|
| **講師ダッシュボード** | `http://localhost:9876/dashboard` |
| **学生用ゲーム** | `https://xxx.trycloudflare.com/dragon.html` |
| **学生用アップロード先**（自動入力） | `https://xxx.trycloudflare.com/status` |
| 全チーム JSON ダウンロード | ダッシュボードの 📥 ボタン |
| 個別チーム復元コマンド | ダッシュボード各行の 📋 ボタン |

---

## 📚 関連ドキュメント

- 暗号ステージ詳細 → [INSTRUCTOR_CIPHER.md](INSTRUCTOR_CIPHER.md)
- 修理ターミナル詳細 → [INSTRUCTOR_REPAIR.md](INSTRUCTOR_REPAIR.md)
- ボス・モブ戦詳細 → [INSTRUCTOR_BOSS.md](INSTRUCTOR_BOSS.md)
- 全体ガイド → [instructor_guide.md](instructor_guide.md)

---

## トラブルシュート Q&A

**Q. `node tracker/server.js` で `EADDRINUSE` エラーが出る**
A. 既に同じポートで何かが動いている。`lsof -i :9876` で確認し、不要なプロセスを `kill <PID>` で停止。

**Q. cloudflared の URL がいつまでも出てこない**
A. ネットワーク接続を確認。ファイアウォール／プロキシが原因の場合あり。

**Q. 📥 ボタンで「サイトでファイルを取得できませんでした」**
A. サーバーが古いバージョンで動いている。ターミナル①で `Ctrl+C` → `node tracker/server.js` で再起動。

**Q. 学生のダッシュボード接続が頻繁に「薄色」になる**
A. クライアントが 1 分以上アップロードしていない。プレイ中なら自動で 30 秒に 1 回送信されるはずなのでネットワーク疎通を確認。

**Q. admin コマンドが効かない**
A. `admin.help()` を実行してエラーになるなら、`js/admin.js` がロードされていない。ページをリロード。

**Q. 同じ URL で再開したいが quick tunnel で毎回変わってしまう**
A. Cloudflare の **名前付きトンネル**（named tunnel）を設定すれば固定 URL になる。要 Cloudflare 管理ドメイン。
